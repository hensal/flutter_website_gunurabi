import 'package:flutter/material.dart';

class News2021Page extends StatelessWidget {
  const News2021Page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "News 2021 Page Content",
        style: TextStyle(fontSize: 20),
      ),
    );
  }
}