import 'package:flutter/material.dart';

class Sustainability extends StatelessWidget {
  const Sustainability({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Sustainability efforts and goals.");
  }
}
