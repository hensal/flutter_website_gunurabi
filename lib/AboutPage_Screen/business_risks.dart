import 'package:flutter/material.dart';

class BusinessRisks extends StatelessWidget {
  const BusinessRisks({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Risks involved in our business.");
  }
}