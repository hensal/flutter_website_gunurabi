import 'package:flutter/material.dart';

class MessagesFromManagementPage extends StatelessWidget {
  const MessagesFromManagementPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Messages from our president and founder.");
  }
}