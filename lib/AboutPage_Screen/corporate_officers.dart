import 'package:flutter/material.dart';

class CorporateOfficers extends StatelessWidget {
  const CorporateOfficers({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Details about our officers.");
  }
}