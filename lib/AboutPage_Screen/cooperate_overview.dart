import 'package:flutter/material.dart';

class CorporateOverview extends StatelessWidget {
  const CorporateOverview({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Overview of our company.");
  }
}