import 'package:flutter/material.dart';

class Organization extends StatelessWidget {
  const Organization({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Organizational structure.");
  }
}