import 'package:flutter/material.dart';

class BusinessLocations extends StatelessWidget {
  const BusinessLocations({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Our business locations.");
  }
}