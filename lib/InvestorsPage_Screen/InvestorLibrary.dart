import 'package:flutter/material.dart';

class InvestorLibrary extends StatelessWidget {
  const InvestorLibrary({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Our investoer library.");
  }
}