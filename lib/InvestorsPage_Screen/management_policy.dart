import 'package:flutter/material.dart';

class ManagementPolicy extends StatelessWidget {
  const ManagementPolicy({super.key});
  @override
  Widget build(BuildContext context) {
    return const Text("Our management policy.");
  }
}